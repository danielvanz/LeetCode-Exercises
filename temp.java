import java.util.HashSet;

public class temp {

    public int backtrack(int[] A, int i, int amountColors, HashSet<Integer> currentColors) {

        if (i == A.length) {
            
        } else {

            if (currentColors.size() == 0) {
                f(i) == 1;
            }

            //Nodes J are all neighbours of node I, the current Node. 
            for (j <- A[i]) {
                if (condition) {
                    
                }
            }


        }

        for (int i = 0; i < nodes.length; i++) {

        }
        return -1;
    }

}